[project]
name = "undergraduate-research"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pymupdf4llm",
    "jsonschema",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mto_benchmark"]

